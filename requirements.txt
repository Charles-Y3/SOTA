--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cpu
faster-whisper>=1.0.3
customtkinter>=5.2.2
# Simplified -> Traditional Chinese conversion for transcripts. The pure-
# Python reimplementation on purpose: identical dictionaries, no native
# wheel to go wrong under PyInstaller on either platform.
opencc-python-reimplemented>=0.1.7
tkinterdnd2>=0.4.2
sounddevice>=0.4.6
python-docx>=1.1.0
# 0.3.19 pinned deliberately: newer wheels (0.3.21+) are compiled with CPU
# instructions that crash on pre-AVX512 processors (illegal instruction).
llama-cpp-python==0.3.19
# SenseVoice engine (Transcribe tab toggle + Live Transcription tab).
# funasr does not declare torch as an install dependency, so it's pinned
# here explicitly. Versions are bare (no "+cpu" local suffix) on purpose:
# that suffix only exists on the Windows/Linux wheels the pytorch.org CPU
# index above serves — macOS wheels for the same version have no local
# suffix at all, so pinning it would break the macOS build. Leaving it off
# still resolves to the ~120MB CPU-only build on Windows/Linux (pip matches
# "==2.13.0" against a "2.13.0+cpu" wheel from that index) and to the
# (already CPU/MPS-only) default macOS wheel.
torch==2.13.0
torchaudio==2.11.0
funasr>=1.3.14
# Audio Studio (v2): DSP filters/EQ/compression for the Enhance subtab and
# resampling in audio_clean.py's VAD-based silence/pause detection.
scipy>=1.11.0
# Audio Studio Enhance's noise reduction: noisereduce's spectral gating —
# the ONLY engine here on purpose (the ML-based option, NSNet2, lives
# exclusively in the Edit tab's AI panel via audio_ai_edit.py, run
# through onnxruntime — already a dependency below/transitively via
# faster-whisper's own VAD filter, so no new heavy runtime is added for
# it). DeepFilterNet was removed from this project entirely: it never
# actually ran here — DeepFilterNet 0.5.6 imports
# torchaudio.backend.common.AudioMetaData, which doesn't exist on the
# torchaudio version pinned above (confirmed by import error, not
# assumed), so audio_denoise.denoise() silently fell back to noisereduce
# on every call regardless of the setting. See git history if it's ever
# worth revisiting (would need either a DeepFilterNet release supporting
# this torchaudio, or bumping torchaudio — which risks the same
# pre-AVX512 crash torch is pinned against elsewhere in this file, so
# needs its own verification pass).
noisereduce>=3.0.0
# NSNet2 (the AI panel's ML noise reduction, audio_ai_edit.py) runs
# through onnxruntime. Already present transitively via faster-whisper's
# own VAD filter, but pinned explicitly here since SOTA's own code now
# imports it directly too — same reasoning as the Pillow pin below.
onnxruntime>=1.17.0
# MP3 export for Audio Studio's finished/edited audio. Pure-Python-
# installable LAME binding — avoids bundling/shelling out to ffmpeg.
lameenc>=1.6.1
# ITU-R BS.1770 loudness measurement/normalization (Enhance's LUFS row) —
# pure Python + numpy/scipy, no compiled extensions.
pyloudnorm>=0.1.1
# Spectrogram rendering (Edit subtab's Waveform/Spectrogram toggle) needs
# PIL.Image/ImageTk. Already present transitively via customtkinter, but
# pinned explicitly rather than relying on another package's transitive
# dependency by accident.
Pillow>=10.0.0
